// Only business-confirmed deployment months belong here, keyed by `${type}:${code}`.
// Reference-zone creation and historical measurement dates are not deployment dates.
export const PUBLIC_STATS_DEPLOYMENT_MONTHS = Object.freeze({})
